#include "Cosine.h"
#include <cmath>
// Implementation of Cosine::execute: takes two inputs but one is already define as zero because only one would be needed
// and returns the number cosine using cos() function from cmath library
double Cosine:: execute(double a , double b){
    return cos(a);
}