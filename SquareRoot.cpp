#include "SquareRoot.h"
#include <cmath>

// Implementation of SquareRoot::execute: takes two inputs but one is already define as zero because only one would be needed
// and returns the number square root using sqrt() function from cmath library
double SquareRoot::execute(double a, double b){
    return sqrt(a);
}